apiVersion: v1
kind: Service
metadata:
  name: {{ include "template_chart.fullname" . }}
  labels:
    {{- include "template_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "template_chart.selectorLabels" . | nindent 4 }}
